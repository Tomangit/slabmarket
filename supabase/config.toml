# Supabase Configuration
# This file configures Supabase CLI for local development

[project]
# Your Supabase project reference ID
# You can find this in your Supabase dashboard
id = "xxsnsomathouvuhtshyw"

[api]
# Port for the API Gateway
port = 54321
# Schemas to expose
schemas = ["public", "storage", "graphql_public"]
# Extra schemas to add to the search_path of every request
extra_search_path = ["public", "extensions"]
# The maximum number of rows returns from a view, table, or stored procedure
max_rows = 1000

[db]
# Port for the local Postgres instance
port = 54322
# Port for the local Postgres pooler
pooler_port = 54329
# The database major version to use
major_version = 15

[studio]
# Port for the Supabase Studio
port = 54323
# External URL of the API server
api_url = "http://127.0.0.1"

[inbucket]
# Port for the local email testing server web interface
port = 54324
# Port for the local email testing server SMTP port
smtp_port = 54325
# Port for the local email testing server POP3 port
pop3_port = 54326

[storage]
# The maximum file size allowed (e.g. "5MB", "500KB")
file_size_limit = "50MiB"

[auth]
# The base URL of your website. Used as an allow-list for redirects and for constructing URLs used
# in emails.
site_url = "http://localhost:3000"
# A list of *exact* URLs that auth providers are permitted to redirect to post authentication.
additional_redirect_urls = ["https://localhost:3000"]
# How long tokens are valid for, in seconds. Defaults to 3600 (1 hour), maximum 604800 (1 week).
jwt_expiry = 3600
# If disabled, the refresh token will never expire.
enable_refresh_token_rotation = true
# Allows refresh tokens to be reused after expiry, up to the specified interval in seconds.
# Requires enable_refresh_token_rotation = true.
refresh_token_reuse_interval = 10
# Allow/disallow new user signups to your project.
enable_signup = true

[auth.email]
# Allow/disallow new user signups via email to your project.
enable_signup = true
# If enabled, a user will be required to confirm any email change on both the old, and new email
# addresses. If disabled, only the new email is required to confirm.
double_confirm_changes = true
# If enabled, users need to confirm their email address before signing in.
enable_confirmations = false

[auth.sms]
# Allow/disallow new user signups via SMS to your project.
enable_signup = true
# If enabled, users need to confirm their phone number before signing in.
enable_confirmations = false

[edge_runtime]
# Configure one of the supported ports: 9000 (default), 9001, or 9002
port = 9000
# Configure one of the supported ports: 9001 (default), 9002, or 9003
inspector_port = 9001

